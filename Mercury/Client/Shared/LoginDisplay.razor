<AuthorizeView>
    <Authorized>
        <RadzenProfileMenu>
            <Template>
                <RadzenGravatar Email="user@example.com">
                </RadzenGravatar>
            </Template>
            <ChildContent>
                <RadzenCard>
                    <RadzenRow>
                        <RadzenImage Path="Images/photo_dani.jpg"></RadzenImage>
                        <RadzenText Text="David Tortosa Codoñer"></RadzenText>
                    </RadzenRow>
                </RadzenCard>
                <RadzenProfileMenuItem Text="Mi perfil" Path="Perfil" Icon="face"></RadzenProfileMenuItem>
                <RadzenProfileMenuItem  Text="Logout" Path="Logout" Icon="exit_to_app"></RadzenProfileMenuItem>
            </ChildContent>
        </RadzenProfileMenu>
    </Authorized>
    <NotAuthorized>
        <RadzenRow AlignItems="AlignItems.End" Wrap="FlexWrap.Wrap" Gap="1rem">
            <NavLink href="Register">
                <RadzenButton Text="Register" ButtonStyle="ButtonStyle.Primary" />
            </NavLink>
            <NavLink   href="Login">
                <RadzenButton Text="Login" ButtonStyle="ButtonStyle.Primary" />
            </NavLink>
        </RadzenRow>
    </NotAuthorized>
</AuthorizeView>
