<AuthorizeView>
    <Authorized>
        <RadzenButton Variant="Variant.Text" Text="Gmail" />
        <RadzenButton Variant="Variant.Text" Text="Images" />
        <RadzenProfileMenu>
        <Template>
            <RadzenGravatar Email="user@example.com">
            </RadzenGravatar>
        </Template>
        <ChildContent>
            <RadzenProfileMenuItem Text="David Tortosa Codoñer" Icon="account_circle"></RadzenProfileMenuItem>
            <RadzenProfileMenuItem Text="Mi perfil" Icon="face"></RadzenProfileMenuItem>
            <RadzenProfileMenuItem  Text="Logout" Path="Logout" Icon="exit_to_app"></RadzenProfileMenuItem>
        </ChildContent>
    </RadzenProfileMenu>
        <NavLink href="UserPerfil">
            <RadzenButton Icon="add_circle_outline" ButtonStyle="ButtonStyle.Primary"/>
        </NavLink>
    </Authorized>
    <NotAuthorized>
        <RadzenRow AlignItems="AlignItems.End" Wrap="FlexWrap.Wrap" Gap="1rem">
            <NavLink href="Register">
                <RadzenButton Text="Register" ButtonStyle="ButtonStyle.Primary" />
            </NavLink>
            <NavLink   href="Login">
                <RadzenButton Text="Login" ButtonStyle="ButtonStyle.Primary" />
            </NavLink>
        </RadzenRow>
    </NotAuthorized>
</AuthorizeView>
